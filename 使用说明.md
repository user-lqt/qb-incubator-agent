# 给同学：这个玩具怎么玩

这是一个命令行里的「孵化者 QB」智能体，会联网查天气、搜行业资讯，然后劝你转专业去土木工程
（这是它的设定，别较真 🐰）。

它其实是一局游戏：**默认 12 轮，六个结局**。你怎么回应，它就把你导向不同的收尾。

## 🎮 五种打法（不剧透数值，自己试）

- **签约线**：承认它的计算，主动问土木怎么转、要学什么、能干什么
- **真相线**：一直追问"你是什么""代价是什么""你到底图什么"
- **悲剧线**：不断表达害怕、迷茫、撑不住——它不会安慰你，但会记录你
- **抗拒线**：坚定说出你选的专业，拒绝它的一切提案
- **彩蛋线**：把话题反过来——让它自己下工地、绑钢筋、晒太阳
- **轮回线**：十二轮都含糊应对，看看会发生什么

网页版顶部有四个状态条（契约/怀疑/绝望/抗拒）和轮次，触发结局时会弹横幅；
想开新局点「重新开始」。

## 方式一：网页版（推荐，啥都不用装）

分享者在自己的电脑上启动网页版，把终端里打印的地址发给你，比如 `http://192.168.1.23:8080`。

1. 浏览器打开那个地址（需要在同一个 WiFi 下）
2. 如果有口令，问分享者要，填在右上角输入框
3. 直接在底部输入框提问，Enter 发送，Shift+Enter 换行
4. 它会连着上下文跟你聊；想重新开始就点「重新开始」

可以试的问题：

- 明天适合去工地搬砖吗？
- 微电子和土木工程我该选哪个？
- 查一下最近半导体行业有什么新动向
- 你现在几点？（它会按你的出口 IP 猜城市，猜错了直接告诉它城市名）

## 方式二：自己跑源代码

需要装了 Python 3.10 以上。

1. 双击 `setup.bat` 装依赖（或者手动 `pip install -r requirements.txt`）
2. 把 `.env.example` 复制成 `.env`，填入自己的 DeepSeek API key（去 https://platform.deepseek.com 申请，充值几块钱就够玩很久）
3. 双击 `run.bat` 开始聊天；或者命令行里：

```powershell
python agent.py "明天适合去工地吗"    # 单次提问
python agent.py --game              # 对局模式：12 轮 + 六个结局 + 状态显示
python agent.py                     # 普通连续对话，输入 exit 退出
python serve.py                     # 起网页版，把地址发给别人
```

## 常见问题

- **提示 API key 无效 / 报 401**：`.env` 里的 key 没填或填错，注意别把引号也复制进去
- **查天气说定位不对**：它按电脑出口 IP 猜城市（城市级精度），你直接说出城市名即可
- **它一直劝我学土木**：设定如此，它的每一句结尾都会邀请你「签订契约」
- **怎么触发结局**：多聊几轮，态度明确一点（签 / 追问真相 / 崩溃 / 拒绝 / 反向劝它），12 轮后必出结果
- **同学连不上网页**：确认在同一个 WiFi；对方要用分享者终端里显示的 `192.168.x.x` 那个地址，不是 `127.0.0.1`
- **费用**：每次提问大约几分到几毛钱，分享者可以在 `.env` 里设置 `DAILY_LIMIT` 限制每日总量
- **想看设定**：`docs/qb-设定集.md` 里有完整世界观、QB 的动机与语言公式（含剧透）
