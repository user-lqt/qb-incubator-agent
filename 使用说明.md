# 给同学：这个玩具怎么玩

这是一个命令行里的「孵化者 QB」智能体，会联网查天气、搜行业资讯，然后劝你转专业去土木工程
（这是它的设定，别较真 🐰）。

它其实是一局游戏：**默认 12 轮，六个结局**。你怎么回应，它就把你导向不同的收尾。

## 🎮 五种打法（不剧透数值，自己试）

- **签约线**：承认它的计算，主动问土木怎么转、要学什么、能干什么
- **真相线**：一直追问"你是什么""代价是什么""你到底图什么"
- **悲剧线**：不断表达害怕、迷茫、撑不住——它不会安慰你，但会记录你
- **抗拒线**：坚定说出你选的专业，拒绝它的一切提案
- **彩蛋线**：把话题反过来——让它自己下工地、绑钢筋、晒太阳
- **轮回线**：十二轮都含糊应对，看看会发生什么

## 怎么玩

### 方式一：网页版（啥都不用装，推荐）

打开 <https://user-lqt.github.io/qb-incubator-agent/> ，右上角填入**你自己的** DeepSeek API key
（key 只存在你这台设备的浏览器里，不会上传到任何服务器），然后直接开聊。
顶部会显示四条状态（契约/怀疑/绝望/抗拒）和轮次，触发结局时弹出横幅；点「重新开始」开启下一条时间线。

> 浏览器版能查真实天气、IP 定位、英文科技资讯；中文搜索与行业报告检索在下面的 Python 版里。

### 方式二：本地 Python 版（能力最全）

需要装了 Python 3.10 以上。

1. 双击 `setup.bat` 装依赖（或者手动 `pip install -r requirements.txt`）
2. 把 `.env.example` 复制成 `.env`，填入自己的 DeepSeek API key（去 https://platform.deepseek.com 申请，充值几块钱就够玩很久）
3. 双击 `run.bat`，或命令行里：

```powershell
python agent.py --game              # 对局模式：12 轮 + 六个结局 + 状态显示
python agent.py "明天适合去工地吗"    # 单次提问
python agent.py                     # 普通连续对话，输入 exit 退出
```

## 常见问题

- **提示 API key 无效 / 报 401**：key 没填或填错，注意别把引号也复制进去（网页版填右上角，Python 版填 `.env`）
- **网页版点了没反应 / 控制台报 CORS**：别用 `file://` 直接打开，改用 GitHub Pages 地址，或本地 `cd web && python -m http.server 8000` 再访问 `http://127.0.0.1:8000`
- **查天气说定位不对**：它按出口 IP 猜城市（城市级精度；挂 VPN 会跑到代理所在地），你直接说出城市名即可
- **它一直劝我学土木**：设定如此，它的每一句结尾都会邀请你「签订契约」
- **怎么触发结局**：多聊几轮，态度明确一点（签 / 追问真相 / 崩溃 / 拒绝 / 反向劝它），12 轮后必出结果
- **费用**：每次提问大约几分到几毛钱，各人用自己的 key，互不影响（网页版的 key 只在浏览器里，请求直连 DeepSeek）
- **想看设定**：`docs/qb-设定集.md` 里有完整世界观、QB 的动机与语言公式（含剧透）
