fix(game): 修"连续几轮无变化"——主回答漏带评分时自动补救判定

问题：模型偶尔忘记在回答末尾附 [[STATE:{...}]]，于是整轮退化成关键词机，
细腻输入（如"我担心这条路走下坡路"）关键词判 0，界面就连续几轮显示"无变化"。

修复：
- 新增 score_only()：跳过人设、不带工具，单独问一次只要一行 JSON；
  主回答没带评分时自动走这条补救路径，score_source 记为 "rescue"
- run_agent 支持 override_system / use_tools，供纯工具性调用（评分器）使用
- 补救调用容错：既认 [[STATE:{...}]] 标记，也认裸 JSON（含被代码块包裹的情况）
- 界面：变化行后追加判定来源（语义判定 / 语义判定·补救 / 关键词兜底），
  一眼看出这轮是谁在打分
- 前端资源版本号 v10 -> v11

实机验证：4/4 用例补救判定稳定（含"嗯"这类应为全 0 的输入）
