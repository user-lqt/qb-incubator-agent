# 统一换行符：仓库内文本文件一律存 LF，避免 Windows 检出 CRLF 后
# 与线上（GitHub Pages / CI）文件出现字节差异。
* text=auto eol=lf

# Windows 批处理必须是 CRLF，否则可能无法双击运行
*.bat text eol=crlf

# 二进制文件不做换行转换
*.png binary
*.jpg binary
*.ico binary
*.zst binary
